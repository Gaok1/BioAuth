## Status

| VLT-02 | P0 | 🧪 | Store Android implementado no canal `bioauth/vault_store`: AES-256-GCM no Android Keystore, autenticação por uso exclusivamente `BIOMETRIC_STRONG`, invalidação por novo enrollment, tentativa StrongBox com fallback apenas para outra implementação do Android Keystore, arquivo cifrado privado e CRUD paginado com revisões otimistas tipadas. Há testes JVM e suíte instrumentada API 35, mas sem execução em hardware/emulador o teto permanece 🧪. |

## Dependências

Nenhuma.

## Ressalvas

Não havia dispositivo ou emulador conectado (`adb devices` vazio), portanto a suíte API 35 foi compilada, mas não executada. Os scripts/JAR do Gradle wrapper também não existem neste worktree; o gate foi executado com o wrapper funcional de `G:\gitthub\BioAuth\mobile\android`, apontando `-p` para `packages/phone_auth_native/example/android` deste worktree.
